"use strict";
document.addEventListener('DOMContentLoaded', () => {
    var _a;
    // SignUp button
    (_a = document.getElementById('regisztracio')) === null || _a === void 0 ? void 0 : _a.addEventListener('click', () => {
        //let user = document.getElementById('felhasznalo').
    });
});
